# 2026-04-14 鸢尾花分类：修复3个报错

## 💡 核心收获
- ✅ `y_pred = model.predict(X_test)` 必须在 `classification_report` 前调用  
- ✅ `iris = load_iris()` 后才能用 `iris.target_names`（函数需调用！）  
- ✅ 用 `classification_report` 替代单一准确率，体现工程思维  

## 📊 实验结果
| 参数 | 准确率 | 结论 |
|------|--------|------|
| `(10,), max_iter=1000` | 96.7% | **最佳** |
| `(100,), max_iter=5000` | 93.3% | 过拟合 |

## 💼 面试话术
“通过单变量实验确定网络结构，在测试集达96.7%。同时用分类报告发现 versicolor 与 virginica 存在特征重叠——这正是非线性模型的价值。”

## 🌱 明日计划
- 尝试 GridSearchCV 自动调参
- 同步到 Gitee 备份（国内访问更快）
